package com.cloudvault.domain;

// JPA entity representing a registered user: id (bigint PK), uuid, email, passwordHash, fullName, role, storageUsed, storageQuota, createdAt, updatedAt, deletedAt.
public class User {
}
