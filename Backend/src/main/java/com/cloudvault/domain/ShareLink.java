package com.cloudvault.domain;

// JPA entity for public share links: id, token (uuid), fileId (FK), createdByUserId (FK), sharePermission, passwordHash, expiresAt, accessCount, createdAt.
public class ShareLink {
}
