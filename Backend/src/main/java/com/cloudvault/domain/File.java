package com.cloudvault.domain;

// JPA entity representing an uploaded file: id, uuid, ownerId (FK users), fileName, mimeType, sizeBytes, s3Key, isDeleted, createdAt, updatedAt, deletedAt.
public class File {
}
