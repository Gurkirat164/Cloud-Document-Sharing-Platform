package com.cloudvault.security;

// NOTE: The CloudVault User entity already implements UserDetails directly,
// so a separate UserPrincipal wrapper class is not needed.
// This file is intentionally left as a placeholder to preserve the package structure.
// If a separate principal class is ever needed, implement it here.
public class UserPrincipal {
    private UserPrincipal() {}
}
