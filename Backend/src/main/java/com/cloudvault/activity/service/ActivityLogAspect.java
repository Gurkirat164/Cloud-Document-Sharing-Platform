package com.cloudvault.activity.service;

/**
 * AOP aspect placeholder.
 *
 * A full AOP aspect implementation would use {@code @Around} to intercept
 * service methods annotated with a custom {@code @Auditable} annotation and
 * automatically call {@link ActivityLogService#log} with the event context.
 *
 * This is intentionally left as a no-op stub — activity logging is currently
 * wired manually inside service methods for clarity and traceability.
 * Promote to a full AOP Aspect when the codebase grows large enough to justify it.
 */
public class ActivityLogAspect {
    // No-op stub — see Javadoc above
}
